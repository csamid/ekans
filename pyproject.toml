[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "ekans"
version = "1.0.0"
readme = "README.md"
requires-python = ">=3.7"

[project.scripts]
play-ekans = "myekans.__main__:main"
